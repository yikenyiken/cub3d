#!/bin/bash

git clone https://github.com/codam-coding-college/MLX42.git mlx
cd ~/goinfre&&
mkdir homebrew && curl -L https://github.com/Homebrew/brew/tarball/master | tar xz --strip 1 -C homebrew&&
brew install cmake && brew install glfw